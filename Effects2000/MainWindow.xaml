<Window x:Class="Effects2000.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:clr="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Effects2000"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <clr:String x:Key="A">Yes</clr:String>
        <clr:String x:Key="B">No</clr:String>
    </Window.Resources>
    <WrapPanel>
        <Button x:Name="button" Content="{Binding Source={StaticResource A}}" Height="100" Width="100" Margin="10"   Command="{Binding YesCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Content}" />
        <Button x:Name="button_Copy" Content="{Binding Source={StaticResource A}}" Height="100" Width="100" Margin="10" Command="{Binding NoCommand}"    />

    </WrapPanel>
    
</Window>
